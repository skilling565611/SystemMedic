# Portable Python Payload

Place the official Windows embeddable Python zip here and rename it to:

```text
python-embed-amd64.zip
```

The bootstrap extracts it into `DevRuntime/Python` and never modifies the global
Windows PATH.
